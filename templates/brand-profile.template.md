# buildmine brand profile

<!-- Filled by /buildmine-setup (brand step). Read by the buildmine-carousel skill so every carousel renders in YOUR brand. Keep it tight. -->

## Identity
- **Brand / handle shown on slides:** (e.g. `@yourhandle`)
- **Masthead label:** (short mono label at the top of every slide, e.g. "BUILD LOG", "FIELD NOTES")
- **Wordmark:** (text wordmark for the footer; note any styling, e.g. "first word full opacity, second word 18% opacity")

## Color tokens
<!-- Hex values. If you have a brand guide, copy them exactly. -->
- **Background (light):** (the main slide background, e.g. #FFFFFF or an off-white)
- **Background (dark):** (used on cover + CTA slides, e.g. #111111)
- **Text on light:**
- **Text on dark:**
- **Accent:** (the one signal color for emphasis, bars, highlights)
- **Accent on light (adjusted):** (a darker version of the accent that passes contrast for text on the light background — leave blank to only use the accent on dark)

## Type
- **Display font:** (headline font; prefer system-available or note the file, e.g. "Georgia, serif")
- **Body font:** (e.g. "system-ui, sans-serif")
- **Label font:** (masthead/footer, e.g. "ui-monospace, Menlo, monospace — uppercase, letterspaced")

## Slide rules
<!-- Non-negotiables for how slides look. -->
- **Cover + CTA slides:** (e.g. dark background, light text)
- **Body slides:** (e.g. light background, dark text)
- **Accent usage:** (e.g. "accent bar left of the key line; accent color on one key word max")
- **Never:** (e.g. gradients, drop shadows, more than 2 fonts, stock photos)
